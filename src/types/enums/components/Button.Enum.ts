export enum ButtonSizes {
    sm = 'sm',
    md = '',
    lg = 'lg',
  }
  
  export enum ButtonTypes {
    reset = 'reset',
    submit = 'submit',
    button = 'button',
  }
  
  export enum ButtonVariant {
    Primary = 'primary',
    Secondary = 'secondary',
    Success = 'success',
  }
  